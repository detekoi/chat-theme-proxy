{
  "name": "twitch-theme-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Gemini API theme generation",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "NODE_ENV=development node --watch index.js",
    "test": "node --check index.js && node --check functions/badge-proxy/index.js && find routes services config middleware utils -name '*.js' -exec node --check {} + && node --test tests/unit/*.test.js"
  },
  "dependencies": {
    "@google-cloud/firestore": "^8.0.0",
    "@google-cloud/storage": "^7.15.0",
    "axios": "^1.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.22.2",
    "express-rate-limit": "^8.5.1",
    "uuid": "^14.0.1"
  },
  "overrides": {
    "@tootallnate/once": ">=3.0.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
